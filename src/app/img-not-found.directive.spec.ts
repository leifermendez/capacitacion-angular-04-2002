import { ImgNotFoundDirective } from './img-not-found.directive';

describe('ImgNotFoundDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgNotFoundDirective();
    expect(directive).toBeTruthy();
  });
});
